/* ============
 * Mutation types for the redirects module
 * ============
 *
 * The mutation types that are available
 * on the redirects module.
 */

export const ADD = 'ADD';
export const CLEAR = 'CLEAR';

export default {
  ADD,
  CLEAR,
};
