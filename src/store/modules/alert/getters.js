/* ============
 * Getters for the redirects module
 * ============
 *
 * The getters that are available on the
 * redirects module.
 */

export default {
  type: ({type}) => type,
  content: ({content}) => content,
  shown: ({visible}) => visible,
};
